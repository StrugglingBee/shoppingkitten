<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shoppingkitten.dao.ResourceDao">

    <!--查询所有的节点-->
    <select id="findAllResource" resultType="com.shoppingkitten.entity.Resource2">
        select * from resource
    </select>

    <!--根据权限ID查找根节点资源-->
    <select id="findResourceByprivilege" resultType="com.shoppingkitten.entity.Resource2">
        select r.* from resource r left join privilege_resource pr on r.resource_id=pr.resource_id where pr.pid=#{pid} and r.parent_id=0
    </select>
    <!--根据父节点查找子节点资源-->
    <select id="findResourceByPid" resultType="com.shoppingkitten.entity.Resource2">
        select * from resource where parent_id=#{parent_id}
    </select>

</mapper>